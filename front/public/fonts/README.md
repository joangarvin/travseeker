# fonts

Fuentes tipográficas servidas como archivos estáticos desde `/fonts/`.

Se cargan en `src/styles/fonts.css` con `@font-face`. Incluyen las familias usadas en la interfaz (sans-serif para UI y serif para titulares).
